# Official docker image.
image: docker:latest

services:
  - docker:dind

before_script:
  - cat ../dockerpswd.txt | docker login -u "louis6docker" --password-stdin louis6docker/react-app

test:
  stage: build
  script:
    - docker build -t louis6docker/react-app -f Dockerfile.dev .
    - docker run -e CI=true louis6docker/react-app npm run test -- --coverage
  only:
    - develop

prod:
    stage: build
    script:
      - docker build -t louis6docker/react-app .
      - docker push  louis6docker/react-app 
    only:
      - production
      - master

